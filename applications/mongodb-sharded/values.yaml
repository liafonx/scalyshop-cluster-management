image:
  debug: true

shards: 2

shardsvr:
  dataNode:
    replicaCount: 2

auth:
  enabled: true
  rootUser: scaly
  rootPassword: scalypw
  replicaSetKey: ldAwsDj3R9
  
# export MONGODB_REPLICA_SET_KEY=$(kubectl get secret --namespace "gitlab-managed-apps" mongodb-sharded -o jsonpath="{.data.mongodb-root-password}" | base64 -d)
# export MONGODB_REPLICA_SET_KEY=$(kubectl get secret --namespace "gitlab-managed-apps" mongodb-sharded -o jsonpath="{.data.mongodb-replica-set-key}" | base64 -d)


