repositories:
  - name: bitnami
    url: https://charts.bitnami.com/bitnami

releases:
- name: mongodb
  namespace: gitlab-managed-apps
  chart: bitnami/mongodb
  version: 16.4.0
  installed: true
  values:
    - values.yaml
