repositories:
  - name: bitnami
    url: https://charts.bitnami.com/bitnami

releases:
- name: mongodb
  namespace: gitlab-managed-apps
  chart: bitnami/mongodb
  version: 8.0.4
  installed: true
  values:
    - values.yaml
